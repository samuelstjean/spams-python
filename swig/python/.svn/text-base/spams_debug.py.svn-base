import spams_wrap

def tst(A):
    B = spams_wrap.tst(0,False,A)
    return B

def xtst(A,flag):
    return spams_wrap.xtst(A,flag)

def ztst(X,ret = False):
    x = spams_wrap.ztst(X,0,0,0,ret)
    if ret:
        (D,A,B,viter) = x
        return (D,A,B,viter[0])
    else:
        return x
